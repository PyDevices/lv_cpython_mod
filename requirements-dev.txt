# Dev / local wheel builds for lv_cpython_mod (not an end-user runtime install).
# Users install the lvgl-cpython wheel; see README.md.
# Requires sibling lv_bindings with generated/lvgl_python.c when regenerating.

-e .
build>=1.0
setuptools>=61
wheel
